# Copy to .env and fill in. .env is git-ignored and must never be committed.
# Both are optional: with no keys the platform runs in demo mode.

ABUSEIPDB_API_KEY=
LLM_PROVIDER=none          # anthropic | openai | gemini | none
LLM_API_KEY=
